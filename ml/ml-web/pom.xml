<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <parent>
        <artifactId>ml-all</artifactId>
        <groupId>org.michaelliu</groupId>
        <version>1.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>ml-web</artifactId>
    <packaging>war</packaging>
    <name>ml-web Maven Webapp</name>
    <url>http://maven.apache.org</url>

    <properties>
        <spring.version>4.3.0.RELEASE</spring.version>
        <servlet.version>3.1.0</servlet.version>
        <jstl.version>1.2</jstl.version>
        <log4j.version>1.2.17</log4j.version>
    </properties>

    <dependencies>
        <!-- dependency on module ml-service -->
        <dependency>
            <groupId>org.michaelliu</groupId>
            <artifactId>ml-service</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- dependency on module ml-rpc-webservice -->
        <dependency>
            <groupId>org.michaelliu</groupId>
            <artifactId>ml-rpc-webservice</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- dependency on module ml-rpc-remote -->
        <dependency>
            <groupId>org.michaelliu</groupId>
            <artifactId>ml-rpc-remote</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- dependency on module ml-rpc-webservice -->
        <dependency>
            <groupId>org.michaelliu</groupId>
            <artifactId>ml-rpc-webservice</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- spring MVC -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webmvc</artifactId>
            <version>${spring.version}</version>
        </dependency>

        <!-- java servlet -->
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
            <version>${servlet.version}</version>
        </dependency>

        <!-- jstl -->
        <dependency>
            <groupId>jstl</groupId>
            <artifactId>jstl</artifactId>
            <version>${jstl.version}</version>
        </dependency>

        <!-- log4j日志打印 -->
        <dependency>
            <groupId>log4j</groupId>
            <artifactId>log4j</artifactId>
            <version>${log4j.version}</version>
        </dependency>
    </dependencies>

    <build>
        <finalName>ml</finalName>
    </build>
</project>
